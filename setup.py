from setuptools import setup, find_packages
from os.path import join, dirname

setup(
    name='noorm',
    version='0.0.1',
    packages=find_packages(),
    long_description="Noorm - write queries pythonic-way with the power of raw sql",
)
